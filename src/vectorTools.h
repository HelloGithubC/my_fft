#ifndef VECTOR_UTILS_H
#define VECTOR_UTILS_H
#include <vector>
#include <string>
#include <fmt/core.h>

template <class T>
void PrintVector(std::vector<T>& vec, std::string fmt);

#endif // VECTOR_UTILS_H